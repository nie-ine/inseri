version: '2.1'
jobs:
  build:
    docker:
      - image: 'cimg/node:16.13'
    resource_class: small
    steps:
      - checkout
      - run: |
          cd backend
          npm ci
          npm run build
          ls -l
workflows:
  backend:
    jobs:
      - build
