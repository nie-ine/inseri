import { TileDropTargetDirective } from './tile-drop-target.directive';

describe('TileDropTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new TileDropTargetDirective();
    expect(directive).toBeTruthy();
  });
});
